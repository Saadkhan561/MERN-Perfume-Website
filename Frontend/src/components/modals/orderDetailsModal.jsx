import React from "react";
import { DialogContent } from "../ui/dialog";

const OrderDetail = ({ clearQueryParam }) => {
  return <DialogContent className="bg-white">Hello</DialogContent>;
};

export default OrderDetail;
